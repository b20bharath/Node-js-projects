/*

This is a Sticky Note application created using Node.js, JavaScript and JSON.

Here we get the notes as an argument in the commands of node and using yargs module we store the data int the JSON file.

*/